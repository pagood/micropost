
<%=  form_for(current_user.active_relationships.find_by(following_id: user.id),html: {method: :delete,id:"form-for-unfollow"},remote: true) do |f|%>
	<div><%= hidden_field_tag :following_id, user.id %></div>
	<%= f.submit "Unfollow",class:"btn btn-default follow-btn text-center" %>
<% end %>